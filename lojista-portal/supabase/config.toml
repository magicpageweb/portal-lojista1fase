# Projeto Supabase de destino do CLI (supabase link / db push).
#
# Deve acompanhar o ref usado em runtime (.env.production, src/config/supabase.demo.ts
# e README). Até 2026-09 este arquivo ainda apontava para "dosuwmurijxkxgouifbx",
# o projeto do scaffold original, que nunca recebeu dados — um `db push` acabava
# aplicando migration no banco errado.
project_id = "finkazcfuadukylmrqyh"
